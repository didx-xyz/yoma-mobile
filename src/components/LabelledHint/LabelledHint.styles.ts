import { StyleSheet, ViewStyle } from 'react-native'

const styles = {
  container: {
    paddingVertical: 8,
    flexDirection: 'row',
  } as ViewStyle,
  icon: {
    marginRight: 4,
  } as ViewStyle,
}

export default StyleSheet.create(styles)
