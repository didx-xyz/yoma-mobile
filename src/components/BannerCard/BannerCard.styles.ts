import { StyleSheet, ViewStyle } from 'react-native'

const styles = {
  container: {
    overflow: 'hidden',
    marginTop: 0,
  } as ViewStyle,
  backgroundImage: {
    position: 'absolute',
    right: 0,
    bottom: 0,
  } as ViewStyle,
}

export default StyleSheet.create(styles)
