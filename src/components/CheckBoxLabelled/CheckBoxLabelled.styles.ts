import { StyleSheet, ViewStyle } from 'react-native'

const styles = {
  container: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    paddingVertical: 10,
  } as ViewStyle,
}

export default StyleSheet.create(styles)
