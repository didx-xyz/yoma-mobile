import { StyleSheet, ViewStyle } from 'react-native'

const styles = {
  container: {
    flexDirection: 'row',
    marginTop: 5,
  } as ViewStyle,
  content: {
    marginLeft: 12,
    flex: 1,
  } as ViewStyle,
}

export default StyleSheet.create(styles)
