import { StyleSheet, TextStyle, ViewStyle } from 'react-native'

const styles = {
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingVertical: 10,
  } as ViewStyle,
  header: {
    marginLeft: 10,
  } as TextStyle,
}

export default StyleSheet.create(styles)
