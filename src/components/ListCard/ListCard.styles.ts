import { StyleSheet, TextStyle, ViewStyle } from 'react-native'

const styles = {
  cardHeader: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingVertical: 10,
  } as ViewStyle,
  label: {
    marginLeft: 10,
  } as TextStyle,
}

export default StyleSheet.create(styles)
