import { StyleSheet, ViewStyle } from 'react-native'

const styles = StyleSheet.create({
  selected: {
    marginRight: 10,
  } as ViewStyle,
  empty: {
    width: 33,
  } as ViewStyle,
})

export default styles
