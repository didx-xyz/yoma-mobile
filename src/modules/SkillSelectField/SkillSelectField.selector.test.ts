// import * as SUT from './SkillsFilter.selectors'

describe('Skills/SkillsFilter/SkillsFilter.selectors', () => {
  describe('', () => {
    it('should', () => {
      // given ...
      // when ...
      // const result = SUT.
      const result = true
      // then ...
      expect(result).toBe(true)
    })
  })
})
