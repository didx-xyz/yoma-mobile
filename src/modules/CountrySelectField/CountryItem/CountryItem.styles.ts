import { StyleSheet, ViewStyle } from 'react-native'

const styles = {
  container: {
    paddingVertical: 8,
    paddingHorizontal: 0,
    flexDirection: 'row',
  } as ViewStyle,
}

export default StyleSheet.create(styles)
