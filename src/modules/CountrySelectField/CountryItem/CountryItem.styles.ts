import { StyleSheet, TextStyle, ViewStyle } from 'react-native'

const styles = {
  container: {
    paddingVertical: 8,
    paddingHorizontal: 0,
    flexDirection: 'row',
  } as ViewStyle,
  flag: {
    paddingRight: 8,
  } as TextStyle,
}

export default StyleSheet.create(styles)
