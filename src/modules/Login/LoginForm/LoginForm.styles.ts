import { StyleSheet, ViewStyle } from 'react-native'

const styles = {
  form: {
    marginHorizontal: 20,
  },
  button: {
    marginVertical: 15,
    alignSelf: 'center',
  } as ViewStyle,
}

export default StyleSheet.create(styles)
